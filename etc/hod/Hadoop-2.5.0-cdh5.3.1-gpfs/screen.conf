#-*- cfg -*-
# vim: ft=cfg

[Unit]
Name=screen
RunsOn=master

[Service]
# We run 'exec bash' at the end so screen doesn't exit upon command completion.
ExecStart=screen -dmS HODclient bash -c "module load Hadoop/2.4.0-seagate; HADOOP_CONF_DIR=$localworkdir/conf; exec bash"
ExecStop=

[Environment]
