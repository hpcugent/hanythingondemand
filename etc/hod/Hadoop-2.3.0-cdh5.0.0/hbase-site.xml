<?xml version="1.0"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
<configuration>
<!-- 
FINAL The directory shared by RegionServers (eg
hdfs://namenode.example.org:8020/hbase) 
-->
<property>
    <name>hbase.rootdir</name>
    <value>hdfs://$hostname:8020/hbase</value>
</property>

<!-- 
FINAL The mode the cluster will be in. Possible values are false: standalone
and pseudo - distributed setups' 'with managed Zookeeper true: fully -
distributed with unmanaged Zookeeper Quorum (see hbase - env.sh)
-->
<property>
    <name>hbase.cluster.distributed</name>
    <value>true</value>
</property>

<!--
Property from ZooKeepers config zoo.cfg.  The port at which the clients will
connect.
-->
<property>
    <name>hbase.zookeeper.property.clientPort</name>
    <value>2222</value>
</property>

<!--
Comma separated list of servers in the ZooKeeper Quorum.  For example,
"host1.mydomain.com,host2.mydomain.com,host3.mydomain.com".By default this is
set to localhost for local and pseudo-distributed modes of operation.  For
a fully-distributed setup, this should be set to a full list of ZooKeeper
quorum servers. If HBASE_MANAGES_ZK is set in hbase-env.sh this is the list
of servers which we will start/stop ZooKeeper on.',
-->
<property>
    <name>hbase.zookeeper.quorum</name>
    <value></value>
</property>

<!--
Property from ZooKeepers config zoo.cfg. The directory where the snapshot is
stored.
-->
<property>
    <name>hbase.zookeeper.property.dataDir</name>
    <value></value>
</property>

<!--
Temporary directory on the local filesystem. Change this setting to point to
a location more permanent than '/tmp' (The '/tmp' directory is often
cleared on machine restart).
-->
<property>
    <name>hbase.tmp.dir</name>
    <value></value>
</property>

<!--
The name of the Network Interface from which a ZooKeeper server should report
its IP address.
-->
<property>
    <name>hbase.zookeeper.dns.interface</name>
    <value></value>
</property>

<!--
The name of the Network Interface from which a region server should report its
IP address.
-->
<property>
    <name>hbase.regionserver.dns.interface</name>
    <value></value>
</property>

<!-- 
The name of the Network Interface from which a master should report its IP
address.
-->
<property>
    <name>hbase.master.dns.interface</name>
    <value></value>
</property>

<!-- Default 1 is too low -->
<property>
    <name>hbase.client.scanner.caching</name>
    <value>100</value>
</property>

</configuration>
